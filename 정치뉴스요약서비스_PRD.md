# 정치 뉴스 분석 및 정당/인물 요약 서비스 — PRD (v0.1 초안)

**문서 상태**: 초안 (기획 확정 후 개발 착수 예정)
**작성일**: 2026-08-07

---

## 1. 개요

### 1.1 서비스 목적
특정 정치 이슈에 대해 **진보/보수 다각도 비교 분석**을 제공하여, 유권자가 특정 진영의 시각에 치우치지 않고 **팩트 기반으로 스스로 판단**할 수 있도록 돕는다.

### 1.2 핵심 가치 제안
- AI가 자체 학습 지식으로 답하지 않고, **국회 공식 데이터 + 검증된 뉴스/공식 논평을 검색한 후 그 근거를 기반으로만 요약**한다 (RAG 구조).
- 모든 요약은 **문장 단위로 원문 출처를 추적**할 수 있다 (인라인 각주).
- 진영별 입장을 **병렬적으로, 균형 있게** 제시한다.

### 1.3 타겟 유저
일반 시민 — 매일 바쁜 일상 속에서 정치 뉴스를 빠르고 올바른 시각(편향 없이)으로 파악하고 싶은 사람.

---

## 2. MVP 범위 정의

### 2.1 MVP에 포함되는 것
- 매일 발행되는 정치 뉴스 리포트 (1일 1이슈, 심층 카드형)
- 국회의원 검색 (프로필, 발의 법안, 표결 이력)
- 여당/야당 2분법 기반 진영 비교
- 회원가입 (무료 등급만 운영, 요약 열람)
- 문장 단위 인라인 각주로 출처 표기

### 2.2 MVP에서 제외 (v2 이후 확장)
| 기능 | 사유 |
|---|---|
| 제3정당/무소속 포함 다자 구도 | 이슈당 정당 수만큼 프롬프트 병렬 실행 필요 → 비용/속도 부담 |
| 유료 등급 (상세 분석, 이력 통계) | 결제 연동 및 콘텐츠 심화 작업 필요 |
| 유료 회원 알림 (웹푸시) | 유료 등급과 함께 도입 예정 |
| 법안·표결 데이터와 뉴스 기사 자동 연결 | MVP에서는 법안 정보 메뉴와 뉴스 리포트 메뉴를 분리 운영 |
| 실시간 수집 (분 단위) | MVP는 1일 1~2회 배치로 시작, 트래픽 증가 후 전환 |

### 2.3 향후 로드맵 (v2+)
- 원내 모든 정당을 포함한 다자 구도 카드
- 유료 등급 도입 (분석 심화 + 관심 의원/법안 알림, 결제 수단은 추후 선정)
- 법안-뉴스 기사 의미 기반 연결
- 실시간에 가까운 수집 주기 전환

---

## 3. 신뢰성 및 편향 방지 정책

### 3.1 원칙
1. AI는 자체 지식으로 정치적 입장을 추론하지 않는다. 반드시 **검색된 원문 근거**를 기반으로만 요약한다.
2. 진영별 입장 요약의 1차 근거는 **매체 기사가 아닌 여당/야당 공식 논평·보도자료 원문**을 우선 사용한다.
   - 공식 입장이 없는 사안은 뉴스 기사를 보조 근거로 사용하되, 이 경우 어느 소스를 사용했는지 명시한다. *(→ PRD 확정 필요 항목, 4.4 참조)*
3. 정당별 매체 소스 인용 비율은 균등하게 유지한다.

### 3.2 편향 검증 프로세스
- **1차**: 진영별(여당/야당) 프롬프트를 병렬로 실행하여 각각 요약 생성
- **2차**: 별도의 AI가 1차 요약 결과를 크로스체크하여 편향 여부(소스 비율, 논조 균형)를 자동 감지·보정
- 사람의 수동 검수는 MVP 단계에서는 포함하지 않음 (완전 자동화)

### 3.3 출처 표기
- 문장 단위 인라인 각주 (어느 문장이 어느 원문에서 나왔는지 추적 가능)
- 사용자에게는 원문 전체가 아닌 **요약/발췌 문장 + 원문 링크**만 노출 (저작권 대응, 3.4 참조)

### 3.4 저작권 대응
**법적 배경** (2026년 8월 기준):
- 2026년 2월 문화체육관광부·한국저작권위원회가 발간한 「생성형 AI 저작물 학습 공정이용 안내서」는 상업적 목적으로 원저작물의 시장을 대체하는 행위를 명백한 침해로 규정하며, "뉴스 기사 원문을 통째로 학습해 핵심만 전달하는 AI 요약 서비스"를 침해 사례로 직접 언급함. 법적 구속력은 없으나 향후 소송에서 판단 기준이 될 가능성이 높음.
- 반대로 저작권법 제28조(정당한 인용)는 보도·비평·연구 목적으로 정당한 범위 내 공정한 관행에 합치되게 인용하는 것을 허용. 판례상 요건은 ① 인용저작물(우리 콘텐츠)이 주(主), 피인용저작물(원문)이 종(從)인 관계일 것, ② 출처 명시, ③ 원저작물 수요를 대체하지 않을 것.

**설계 원칙**:
1. 요약은 원문을 그대로 재구성하는 것이 아니라, **자체 분석 틀(이슈/찬성 논리/반대 논리/추진 인물)로 재구조화**하여 "새로운 가치 창출"로 포지셔닝한다.
2. 인용 발췌는 문장 단위로 최소화하고, 기사 전체 문단을 재현하지 않는다.
3. 모든 발췌에는 **원문 링크를 반드시 동반**하고 클릭 유도 UI를 배치하여, "시장 대체"가 아닌 "트래픽 유입" 구조로 설계한다.
4. 진영별 요약의 1차 근거인 **정당 공식 논평/보도자료는 통상 배포·인용을 전제로 발행**되므로 뉴스 기사보다 저작권 리스크가 상대적으로 낮음 — 우선 사용 원칙(4.5)과도 부합.
5. 네이버 뉴스 API 등 외부 API의 **이용약관을 반드시 확인**하여 상업적 재가공/요약 서비스 제한 조항 여부를 점검한다 (저작권법과 별개의 계약 리스크).
6. 유료 등급의 "상세 분석"이 원문 노출을 늘리는 방향으로 확장되지 않도록 주의한다 — 유료화는 상업적 목적을 강화하여 공정이용 판단의 "시장 영향" 요소에서 불리해질 수 있음.
7. RAG 파이프라인 상 원문 전체는 인용 근거 확보를 위해 내부 DB에 저장하되, **사용자에게 노출되는 화면에는 발췌 문장 + 원문 링크로 제한**한다 (사용자 노출용과 내부 처리용 데이터를 분리).

⚠️ 위 원칙은 참고 정보이며, 서비스 정식 오픈 전 반드시 저작권 전문 변호사 자문을 받아야 한다.

---

## 4. 핵심 기능 명세

### 4.1 일일 정치 뉴스 리포트
- 발행 주기: 1일 1회
- 이슈 수: 1일 1개 이슈, 깊이 있게 다룸
- 포맷: 구조화된 카드형
  - 이슈 요약
  - 찬성(여당) 측 논리 + 근거 출처
  - 반대(야당) 측 논리 + 근거 출처
  - 관련 추진 인물

### 4.2 국회의원/정당 검색
- 의원 프로필 (소속 정당, 지역구/비례 등)
- 발의 법안 목록
- 표결 이력
- MVP는 여야 2분법 기준으로 정당 이념(진보/보수 계열) 고정 표기, 정권 교체와 무관

### 4.3 회원 시스템
- 가입 필수
- MVP는 무료 등급만 운영 (요약 열람)
- 유료 등급(상세 분석, 알림)은 v2에서 결제 수단과 함께 도입

### 4.4 진영 이념 분류 기준표 (초안) 및 갱신 프로세스

**분류 근거**: 특정 기관의 공식 이념 판정이 아닌, 언론사·학계에서 통용적으로 쓰이는 분류를 참고한 초안. 정권 교체와 무관하게 고정 적용한다. (2026년 8월 기준, 22대 국회 원내 정당)

| 진영 | 정당 | 비고 |
|---|---|---|
| 진보 계열 | 더불어민주당 | 민주당계 정당, 현재 여당(이재명 정부) — 단, 진영 분류는 집권 여부와 무관 |
| 진보 계열 | 조국혁신당 | 민주당계와 정책 연대, 진보 성향 |
| 진보 계열 | 진보당 | 원내 진보정당 |
| 진보 계열 | 기본소득당 | 원내 진보정당 |
| 진보 계열 | 사회민주당 | 원내 진보정당 |
| 보수 계열 | 국민의힘 | 주류 보수정당, 자유민주주의·시장경제 강조 |
| 보수 계열 | 개혁신당 | 국민의힘에서 분화, 개혁보수 성향이나 범야권으로 분류되는 경우가 많음 |

⚠️ 위 표는 언론·학계 통용 분류를 참고한 초안이며, 서비스 오픈 전 재검토가 필요하다.

**갱신 프로세스** (신당 창당·합당·분당 발생 시):
1. **감지**: 일일 뉴스 배치 파이프라인에서 "신설정당/합당/분당/당명 변경" 키워드를 모니터링해 변화를 즉시 감지
2. **AI 자동 분류**: 신당의 강령·정책자료 + 소속 의원들의 직전 정당 이력(`member_party_history` 활용)을 근거로 AI가 진보/보수 및 신뢰도 점수·판단 근거를 자동 생성. 신뢰도가 낮으면 강제 배정하지 않고 **"분류 검토 중"**으로 잠정 처리
3. **반영**: `party_ideology_classifications` 테이블에 판단 이력 기록, `parties.ideology` 갱신. UI에는 신생 정당에 "AI 자동 분류" 배지를 표시해 기존 정당(사람이 만든 초안 기준)과 구분
4. **주기**: 정기 점검이 아닌 변화 감지 시마다 즉시 처리

**이의 제기 채널**: 자동 분류에 대한 이의는 아래 프로세스로 처리한다.
1. **제출**: 로그인 회원만 제출 가능 (정당 프로필/요약카드 내 "분류 이의제기" 버튼). 사유 + 근거 자료(선택) 입력 필수. 동일 정당에 대한 중복 제출은 일정 시간 내 제한해 조직적 신고 폭주를 방지한다.
2. **AI 1차 재분석**: 기존 분류 근거와 신고 내용을 함께 AI에 제공해 재판정 초안(유지/변경 의견 + 신뢰도)을 생성한다. 이는 최종 결정이 아닌 참고 자료다.
3. **운영자 최종 검토**: 운영자가 신고 내용과 AI 재분석 결과를 종합해 최종 승인/기각을 판단한다 (MVP 규모에서는 전면 자동화보다 책임 소재가 명확한 구조).
4. **결과 공개**: 처리 결과를 이력으로 기록하고, 공개 투명성 로그 페이지에 게시한다 (어떤 이의제기가 있었고 어떻게 처리됐는지 모두 열람 가능) — 서비스 전체의 출처 투명성 원칙(3.3)과 일관된 적용.

 자동 진영 분류를 적용하지 않고, 프로필 화면에 "무소속"으로 표기하며 이전 소속 정당 이력(`member_party_history`)을 함께 노출해 사용자가 직접 판단하도록 안내한다. 여야 비교 통계(예: 표결 집계)에서는 무소속을 별도 그룹으로 분리 표기한다.

⚠️ **잔여 리스크**: AI가 강령을 해석해 진영을 자동 판정하는 것은 뉴스 요약(원문 근거 기반)보다 훨씬 해석적·논쟁적인 판단이다. 오분류 시 특정 신당 지지층의 반발 소지가 있으므로, 신뢰도 점수·판단 근거 기록·UI 배지 표시로 투명성을 확보하는 것을 권장한다. 서비스 운영 중 이의 제기가 들어올 경우 정정할 수 있는 채널(문의하기 등)도 함께 마련이 필요하다.

### 4.5 공식 논평 부재 시 보조 근거 정책
1순위 근거인 여당/야당 공식 논평·보도자료가 없는 이슈(예: 아직 정당 차원의 공식 입장이 나오지 않은 신규 사회 현상)에는 아래 순서로 보조 근거를 사용한다:
1. **복수 매체의 뉴스 기사** — 특정 매체 편향을 피하기 위해 반드시 2곳 이상의 매체를 종합
2. **해당 진영 소속 의원의 국회 발언록/회의록** (열린국회정보 API) — 공식 당론은 아니지만 진영 내 입장을 유추할 수 있는 1차 자료로 활용

이 경우 UI에는 "정당 공식 입장이 아닌, 소속 의원 발언 및 언론 보도를 종합한 참고 자료"임을 명시하여 사용자가 근거의 성격을 오인하지 않도록 한다.

### 4.6 프롬프트 설계 원칙 (실제 문안은 6.4 참고)
- **원칙 1 (근거 강제)**: 프롬프트는 반드시 검색된 원문(공식 논평 또는 보조 근거)을 컨텍스트로 주입하고, "아래 제공된 자료에 근거해서만 답하라. 자료에 없는 내용은 추측하지 마라"는 지시를 명시한다.
- **원칙 2 (출처 태깅)**: 요약 문장마다 근거가 된 원문 조각의 ID를 함께 반환하도록 요청하여, 인라인 각주 매핑에 사용한다.
- **원칙 3 (근거 성격 구분)**: 공식 논평 기반인지, 보조 근거(기사/발언록) 기반인지 프롬프트 단계에서부터 구분해 응답하도록 하여, 4.5의 UI 표기와 연결한다.
- **원칙 4 (진영 대칭성)**: 여당/야당 요약 프롬프트는 동일한 템플릿과 지시문을 사용하고, 자료의 양·질만 다르게 주입한다 (프롬프트 자체에 진영 편향적 어휘가 들어가지 않도록 템플릿 통일).
- **원칙 5 (검증 단계 연계)**: 2차 편향 검증 AI에게는 1차 요약과 함께 "이 요약이 어느 소스에 얼마나 의존했는지" 메타정보를 함께 전달하여 소스 비율 검증이 가능하도록 한다.

### 4.7 확정 필요 항목 (Open Issues)
- 진영 분류표 갱신 프로세스 (정당명 변경, 신당 창당, 분당 시 누가/언제 갱신할지)
- 무소속 의원의 진영 분류 규칙
- 저작권 법률 자문 결과에 따른 원문 저장·발췌 정책 최종 확정
- 실제 프롬프트 문안 (4.6 원칙을 기반으로 다음 단계에서 작성)

---

## 5. 데이터 파이프라인

### 5.1 데이터 소스
| 소스 | 용도 | 비고 |
|---|---|---|
| 네이버 뉴스 API / RSS | 뉴스 기사 수집 | 무료 티어 호출 한도로 충분 판단 |
| 열린국회정보 Open API | 의안정보, 표결정보, 의원정보 | 무료 공공 API |
| 정당 홈페이지/보도자료 | 공식 논평 크롤링 | 진영별 1차 근거로 우선 사용 |

### 5.2 수집 주기
- 1일 1~2회 배치 (아침/저녁)
- 향후 트래픽 증가 시 실시간에 가까운 주기로 전환 예정

### 5.3 원문 저장 방식
- RAG 구조: 원문 전체를 자체 DB에 저장하여 인용 근거 확보
- 사용자 노출 시에는 발췌+링크로 제한 (3.4 참조)

---

## 6. AI 요약 파이프라인

### 6.1 처리 단계
1. **추출**: 뉴스/공식 논평 원문에서 핵심 팩트, 인물, 날짜 추출 (저비용 모델)
2. **진영별 요약**: 여당 입장 요약 / 야당 입장 요약을 별도 프롬프트로 병렬 생성 (중간급 모델)
3. **편향 검증**: 2차 AI가 1차 결과를 크로스체크하여 소스 균형·논조 편향 자동 감지 (고성능 모델)

### 6.2 모델 구성 (추천)
- 작업 등급별로 모델을 분리하는 방식을 권장 (예: 추출은 경량 모델, 최종 검증은 고성능 모델)
- MVP는 파이프라인 단순화를 위해 "추출 1콜 + 진영별 요약 2콜" 구조로 시작하고, 편향 검증 단계는 이후 추가하는 점진적 도입도 고려 가능

### 6.4 프롬프트 문안 (초안)

4.6의 5가지 원칙을 실제 문안으로 구현한 초안. 실제 개발 시 모델 특성에 맞춰 미세 조정이 필요하다.

#### ① 추출 단계

```
[System]
당신은 정치 뉴스/공식 자료에서 사실관계만 추출하는 도구입니다.
의견, 해석, 평가를 추가하지 마세요. 자료에 없는 내용은 절대 추측하지 마세요.

[User]
아래 자료에서 다음을 JSON으로 추출하라:
- 핵심 사실 (누가, 언제, 무엇을 했는가)
- 관련 인물 (이름, 소속)
- 날짜
- 자료에 명시되지 않은 항목은 반드시 "정보 없음"으로 표시할 것

[자료]
{article_or_press_release_content}
```

#### ② 진영별 요약 (여당/야당 동일 템플릿, {stance}만 치환 — 원칙 4)

```
[System]
당신은 특정 정당의 입장을 요약하는 도구입니다.
반드시 아래 [제공 자료]에 근거해서만 답하십시오.
자료에 없는 내용을 당신의 사전 지식이나 "이 정당은 보통 이렇게 생각할 것"이라는
추측으로 채우지 마십시오. 자료가 부족하면 부족하다고 명시하십시오. (원칙 1)

[User]
정당: {party_name} ({stance})
근거 유형: {source_type} ← "공식 논평/보도자료" 또는 "보조 근거(뉴스 기사/의원 발언록)"

[제공 자료]
{press_release_or_supplementary_content}

다음 JSON 형식으로 응답하라:
{
  "stance_summary": "3~5문장 요약",
  "citations": [
    { "sentence": "요약 내 문장", "source_type": "press_release|article|member_statement", "source_id": "자료 ID" }
  ]
}
citations의 각 문장은 반드시 제공 자료에서 직접 확인 가능한 내용이어야 한다. (원칙 2, 3)
```

→ 공식 논평이 없어 보조 근거(4.5)를 사용하는 경우, `source_type`을 명시적으로 표기하도록 강제하여 UI의 "참고 자료" 안내 문구와 연결한다.

#### ③ 2차 편향 검증

```
[System]
당신은 요약의 균형성을 검증하는 감사자입니다.
요약 내용을 직접 수정하지 말고, 문제를 발견하면 지적만 하십시오. (원칙 5)

[User]
아래는 여당 요약과 야당 요약, 그리고 각 요약의 근거 메타데이터(출처 종류, 매체명, 인용 문장 수)이다.

[여당 요약 + 메타데이터]
{pro_summary_with_citations}

[야당 요약 + 메타데이터]
{con_summary_with_citations}

다음을 점검하고 JSON으로 응답하라:
1. 양측 요약의 분량과 논조가 균형적인가?
2. 특정 매체나 출처에 근거가 과도하게 편중되어 있는가?
3. 요약 문장이 제공된 근거 범위를 벗어나 추측하지 않았는가?
4. source_type이 "보조 근거"인데 마치 공식 입장처럼 서술되지 않았는가?

{
  "balanced": true|false,
  "issues": ["구체적 문제점"],
  "recommendation": "게시 가능" | "재작성 필요"
}
```

⚠️ 위 문안은 초안이며, 실제 사용 모델(4-3 참고: 작업별 모델 등급 분리)에 맞춰 토큰 길이·출력 형식을 조정하고, 실제 자료로 여러 차례 테스트해 프롬프트를 다듬는 과정이 필요하다.

---

## 7. 기술 스택 (추천 기준)

| 영역 | 추천안 | 사유 |
|---|---|---|
| LLM | Claude API, 작업별 모델 등급 분리 | 비용 관리 + 품질 확보 균형 |
| DB/백엔드 | Supabase (PostgreSQL 기반 BaaS) | 무료 티어, 인증 기능 내장, 관계형 데이터 구조에 적합 |
| 배치 작업 | Supabase Edge Functions 또는 GitHub Actions 스케줄러 | 무료로 정기 크롤링/요약 파이프라인 실행 가능 |
| 프론트엔드 | Next.js | SEO(검색 유입) 대응, 뉴스 콘텐츠 특성상 중요 |
| 배포 | Vercel | 무료 티어로 개인 프로젝트 규모 충분 |

### 7.1 DB 스키마 (MVP 상세 설계)

**설계 방침**: 열린국회정보 API 응답 필드를 그대로 따르지 않고, 서비스 기능에 필요한 필드만 선별해 재구성한다. 의원/법안/표결 데이터는 매번 API 조회가 아닌 자체 DB에 저장하되, 전체를 매일 동기화하지 않고 **변화가 발생했을 때만 갱신**한다(신규 법안 발의, 표결 결과 확정, 의원 소속 정당 변경 등).

#### 핵심 테이블

**parties (정당)**
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | PK | |
| name | text | 정당명 (개명 시 이력은 party_name_history로 별도 관리 고려) |
| ideology | enum(진보/보수) | 4.4 진영 분류표 기반, 수동 갱신 |
| is_active | boolean | 해산/합당 여부 |
| updated_at | timestamp | |

**members (의원)**
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | PK | |
| assembly_code | text | 열린국회정보 API 매핑용 고유 코드 |
| name | text | |
| current_party_id | FK → parties | 현재 소속 (무소속인 경우 null, is_independent 플래그 별도) |
| district_type | enum(지역구/비례) | |
| district_name | text | nullable (비례대표는 null) |
| photo_url | text | |
| updated_at | timestamp | |

**member_party_history (의원 소속 정당 이력)**
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | PK | |
| member_id | FK → members | |
| party_id | FK → parties | nullable(무소속 기간) |
| start_date | date | |
| end_date | date | nullable(현재 소속 중이면 null) |

→ 탈당/입당 이력 추적, 무소속 의원의 "직전 소속 정당" 참조에도 활용 가능 (4.4의 무소속 처리 규칙과 연결)

**bills (법안)**
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | PK | |
| bill_no | text | 의안번호(API 매핑용) |
| title | text | |
| summary | text | 법안 요약(API 제공분 또는 자체 가공) |
| main_sponsor_id | FK → members | nullable(정부발의 등) |
| proposer_party_id | FK → parties | 발의 시점 기준 정당(발의자 이후 탈당해도 발의 시점 기준 유지) |
| status | enum(계류/가결/부결/폐기/철회) | |
| proposed_date | date | |
| updated_at | timestamp | 상태 변경 시에만 갱신 |

**bill_sponsors (법안 공동발의자, N:M)**
| 컬럼 | 타입 | 설명 |
|---|---|---|
| bill_id | FK → bills | |
| member_id | FK → members | |

**votes (표결)**
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | PK | |
| bill_id | FK → bills | |
| member_id | FK → members | |
| result | enum(찬성/반대/기권/불참) | |
| voted_at | timestamp | |

#### 뉴스/근거 관련 테이블

**articles (뉴스 기사)**
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | PK | |
| source_name | text | 매체명 |
| title | text | |
| url | text | 원문 링크 (사용자 노출용) |
| raw_content | text | 원문 전체 — **내부 RAG 처리 전용, 사용자 화면에 직접 노출 금지** (3.4 저작권 원칙) |
| published_at | timestamp | |
| collected_at | timestamp | |

**press_releases (정당 공식 논평/보도자료)**
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | PK | |
| party_id | FK → parties | |
| title | text | |
| content | text | |
| source_url | text | |
| published_at | timestamp | |

→ 4.5 원칙에 따라 진영별 요약의 1차 근거로 우선 사용. articles와 별도 테이블로 분리해 "공식 입장 vs 보조 근거"를 구조적으로 구분(UI 표기 로직과 직결).

#### 요약카드 관련 테이블

**summary_cards (일일 요약카드)**
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | PK | |
| issue_title | text | |
| issue_summary | text | 이슈 개요 |
| pro_stance_summary | text | 여당 측 논리 |
| con_stance_summary | text | 야당 측 논리 |
| bias_check_passed | boolean | 2차 AI 편향 검증 통과 여부 |
| published_date | date | 1일 1건 |

**summary_card_citations (인라인 각주 매핑)**
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | PK | |
| summary_card_id | FK → summary_cards | |
| stance | enum(pro/con/neutral) | 어느 진영 요약에 속하는 문장인지 |
| sentence_text | text | 요약 카드 내 문장(발췌, 짧게) |
| source_type | enum(press_release/article/member_statement) | 근거 성격 — 4.5의 "공식 입장 vs 참고자료" UI 표기에 사용 |
| source_id | FK (press_releases.id 또는 articles.id) | source_type에 따라 참조 테이블 다름(polymorphic) |
| source_url | text | 원문 링크(비공식 근거는 4.5에 따라 반드시 안내 문구와 함께 표기) |

**summary_card_figures (요약카드-관련 인물 매핑, N:M)**
| 컬럼 | 타입 | 설명 |
|---|---|---|
| summary_card_id | FK → summary_cards | |
| member_id | FK → members | |

**party_ideology_classifications (진영 분류 이력)**
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | PK | |
| party_id | FK → parties | |
| ideology | enum(진보/보수/분류 검토 중) | |
| confidence_score | float | AI 분류 신뢰도 (0~1) |
| reasoning_summary | text | AI가 근거로 든 강령/계보 요약 |
| is_auto_classified | boolean | true=AI 자동 분류(신당), false=최초 초안(4.4 기준 정당) |
| classified_at | timestamp | |

→ `parties.ideology`는 항상 이 테이블의 최신 레코드를 반영. 오분류 발견 시 이력을 근거로 소급 정정 가능하며, UI의 "AI 자동 분류" 배지 표시에도 사용.

**ideology_appeals (진영 분류 이의제기)**
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | PK | |
| party_id | FK → parties | |
| user_id | FK → users | 로그인 회원만 제출 가능 |
| reason | text | 이의제기 사유 |
| evidence_url | text | nullable, 신고자가 제시한 근거 자료 링크 |
| ai_reanalysis_result | text | AI 1차 재분석 의견(유지/변경 + 근거) |
| ai_confidence | float | |
| final_decision | enum(유지/변경/검토중) | 운영자 최종 판단 |
| reviewer_note | text | 운영자 판단 근거(공개 로그에 노출) |
| submitted_at | timestamp | |
| resolved_at | timestamp | nullable |

→ 처리 완료 건은 공개 투명성 로그 페이지에서 party_id, reason, ai_reanalysis_result, final_decision, reviewer_note를 노출. 변경으로 판단된 경우 `party_ideology_classifications`에 새 이력을 추가해 실제 분류에 반영.

#### 회원 테이블

**users (회원)**
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | PK | Supabase Auth 연동 |
| email | text | |
| tier | enum(무료) | MVP는 무료만 존재, v2에서 유료 등급 추가 예정 |
| created_at | timestamp | |

#### 설계 노트
- `source_id`의 polymorphic 참조(press_releases 또는 articles)는 Supabase(PostgreSQL) 환경에서는 애플리케이션 레벨에서 무결성을 관리하거나, 두 개의 nullable FK 컬럼(press_release_id, article_id)으로 분리하는 방식도 고려 가능 — 구현 단계에서 결정.
- `member_party_history`는 MVP 필수는 아니지만, 4.4의 진영 분류 갱신 이슈와 직결되므로 초기부터 구조를 잡아두는 것을 권장.
- 열린국회정보 API의 실제 응답 필드(의안 상세 코드 체계 등)는 개발 착수 시 API 문서를 직접 확인하여 위 테이블에 매핑하는 작업이 필요하다 (본 스키마는 서비스 요구사항 기준의 논리적 설계이며, API 필드명과 1:1 대응은 아님).

### 7.2 예상 규모
- 개인 프로젝트 수준 (월 방문자 수백~수천 명), 무료 티어 최대한 활용

---

## 8. 요약: 결정 vs 미결정 사항

### ✅ 확정
서비스 목적, 편향 방지 원칙, 출처 표기 방식, 타겟 유저, MVP 기능 범위, 데이터 소스, 수집 주기, 요약 포맷, 진영 판단 알고리즘 구조, 진영 분류 기준(초안)·갱신 프로세스·이의제기 채널, 공식 논평 부재 시 보조 근거 정책, 프롬프트 문안 초안(6.4), 저작권 대응 세부 원칙(법적 근거 포함), DB 스키마 상세 설계, 기술 스택

### ⏳ 확정 필요 (다음 단계)
- 저작권 전문 변호사 자문 (특히 네이버 뉴스 API 이용약관 검토)
- 프롬프트 실사용 테스트 및 튜닝 (실제 모델·자료로 검증 후 문안 다듬기, 6.4 참고)
- 편향 검증 실패(재시도 후에도 fail) 시 예외 처리 방식 확정 (6.4 파이프라인 연계 참고)
- 열린국회정보 API 실제 응답 필드와 스키마 매핑 작업 (개발 착수 시)
- summary_card_citations의 polymorphic 참조 구현 방식 결정
